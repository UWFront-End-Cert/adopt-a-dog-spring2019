
// Add to the end of the array
.push();

//Pop the end of the array off
.pop()

// remove from middle of array, and how many elements
.splice(index, count)

// Turn a list into a string
.join(' ');

//Turns a string into a list
.split(' ')

//
.sort();

.forEach(function(food) {
  console.log(food);
})
