function murphy() {
  let dogName = ['Murphy'];
  const dogPrice = 123.45;
  const dogResult = dogName + ", Great Dane, costs $" + String(dogPrice) + ' to adopt.';

  alert(dogResult);
}

function poppy() {
  let dogName = ['Poppy'];
  const dogPrice = 123.45;
  const dogResult = dogName + ", Maltese, costs $" + String(dogPrice) + ' to adopt.';

  alert(dogResult);
}

function jack() {
  let dogName = ['Jack'];
  const dogPrice = 123.45;
  const dogResult = dogName + ", Beagle, costs $" + String(dogPrice) + ' to adopt.';

  alert(dogResult);
}

function duffy() {
  let dogName = ['Duffy'];
  const dogPrice = 123.45;
  const dogResult = dogName + ", American Staffordshire Terrier, costs $" + String(dogPrice) + ' to adopt.';

  alert(dogResult);
}

function lucas() {
  let dogName = ['Lucas'];
  const dogPrice = 123.45;
  const dogResult = dogName + ", Shepherd Mix, costs $" + String(dogPrice) + ' to adopt.';

  alert(dogResult);
}

function jake() {
  let dogName = ['Jake'];
  const dogPrice = 123.45;
  const dogResult = dogName + ", yellow lab, costs $" + String(dogPrice) + ' to adopt.';

  alert(dogResult);
}

function angus() {
  let dogName = ['Angus'];
  const dogPrice = 123.45;
  const dogResult = dogName + ", boxer, costs $" + String(dogPrice) + ' to adopt.';

  alert(dogResult);
}

function violet() {
  let dogName = ['Violet'];
  const dogPrice = 123.45;
  const dogResult = dogName + ", Chocolate Lab, costs $" + String(dogPrice) + ' to adopt.';

  alert(dogResult);
}

function piper() {
  let dogName = ['Piper'];
  const dogPrice = 123.45;
  const dogResult = dogName + ", Brittany Spaniel, costs $" + String(dogPrice) + ' to adopt.';

  alert(dogResult);
}

function maximus() {
  let dogName = ['Maximus'];
  const dogPrice = 123.45;
  const dogResult = dogName + ", terrier mix, costs $" + String(dogPrice) + ' to adopt.';

  alert(dogResult);
}

function luna() {
  let dogName = ['Luna'];
  const dogPrice = 123.45;
  const dogResult = dogName + ", yellow lab, costs $" + String(dogPrice) + ' to adopt.';

  alert(dogResult);
}

function stella() {
  let dogName = ['Stella'];
  const dogPrice = 123.45;
  const dogResult = dogName + ", chihuahua, costs $" + String(dogPrice) + ' to adopt.';

  alert(dogResult);
}
